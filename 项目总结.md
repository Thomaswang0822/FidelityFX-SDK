# FSR Offline Runner 项目总结

王昊轩，<haoxuan.wang@mthread.com>

## 项目简介

MTSS团队的需求为一个offline runner用来测试AMD FSR的表现。具体为：程序接受我们的inputs，输出经过FSR的Super Resolution和Frame Generation后的生成帧并保存为截图。

实现过程可概括为：在AMD FidelityFX SDK ([repo link](https://github.com/GPUOpen-LibrariesAndSDKs/FidelityFX-SDK)) 的基础上，读取我们的test inputs并将render pipeline中对应的buffer替换，使FSR输出我们要的结果。

## 关键概念

- AMD FidelityFX SDK: 即上述repo, 包含AMD自家的多种render techniques, 和一个self-contained 示例应用，一个应用了SDK中各种功能的Scene Renderer.
- FSR: 全程为FidelityFX Super Resolution. 为SDK中最重要的组成部分。FSR 从3.0版本开始在Super Resolution（超分辨率，ss，upscale，etc.）的基础上增加支持帧生成（插帧， Frame Generation, Frame Interpolation, FG, etc.），与Nvidia DLSS 3类似。FSR 4.0增加了多帧生成的支持，与DLSS 4类似。
- 渲染帧与生成帧：rendered/real frame and generated frame. 在FG开启情况下，渲染帧为render pipeline正常输出的frame，同时作为SS和FG的input。SS和FG先后执行，输出则为生成帧。
- 测试场景：使用一个传统Renderer输出的N个渲染帧作为输入，查看在有FSR support下的N个生成帧（FSR4则可以是2N个）。
- 程序输入：对于一个场景下的连续N个渲染帧，我们需要
  - N个frame capture, 也就是final colors
  - N个motion vectors (mv)
  - N个GBuffer depths (depth)
- Hack: 指将FSR原本的input buffers替换为我们test inputs的行为。
- Render Module (RM): 整个graphics pipeline中执行一系列相关运算的集合，例如ToneMapping RM, UI RM, etc. Nvidia代码库（例如Falcor）中对应的概念为Render Pass. 我们关心的SS和FG一先一后都在`FSRRenderModule`中
- 不同的分辨率：我们有三种分辨率，分别是render resolution， display resolution，monitor resolution。其中monitor res为固定的硬件参数不可改变，render res和display res分别为SS前后的分辨率。三者的取值都可以不同，例如分别为1K, 4K, 2K,意思是“只花1k的render work，让FSR超分到4K, 但是手头只有2K的显示器查看结果”。

其中mv和depth分别为`float2`和`float`,所以二者被encode在同一个RGB image中。因此最终，程序输入为N张capture image和N张 mv + depth image，格式为HDR的exr.

## 程序功能

Offline Runner已被push到我们的库中。TODO: add url.
其中基于SDK 1.1.4和SDK 2.0.0的实现分别在不同的branch下

（这里多解释一下，项目开始时SDK repo最新的版本是1.1.4, 也就是只有FSR3. 在基本完成时更新了SDK并，但似乎FSR4没有被同步放出，详见[issue](https://github.com/GPUOpen-LibrariesAndSDKs/FidelityFX-SDK/issues/164#issuecomment-3216556061).）

项目对于SDK的修改没有区别，同时鉴于SDK 1.1.4的project layout更好（更多源代码，同时包括所有的`CMakeLists.txt`，而2.0.0则只提供了一个VS solution），我们更推荐使用此branch。如果之后FSR4被添加进了SDK，我们会在2.0.0 branch中同步更新。

程序支持两种方法指定hack-related options，分别是json config file和Cmdline args。
具体options有EnableHack，Identifier，RenderResolution，ParseJitter，HackPaths，StoreOutput，OptionalFrameCount，OutputPath.

具体细节请参考repo root下[ourREADME.md](./ourREADME.md)中的[Runtime Options Setup](./ourREADME.md#runtime-options-setup)

SDK原本的各种功能，如runtime options，可以参考[README.md](./README.md)和**docs/**下的guide.

根据hack options中指定的位置，数量，identifier，Offline Runner会进行相应的输出。

## 项目实现流程

本项目耗时约一个月，具体可以分为如下几个大步骤

1. Build SDK并理解基本的结构和resource management.
2. 充分理解Texture class,并在原有load接口（通过`stb_image`实现，仅支持读取LDR image）的基础上添加HDR image的读取，通过`tinyexr`实现。
3. 解决exr load bug, 具体为在store data to texture时使用`cauldron::ResourceFormat`中正确的格式. 此过程需要在`tinyexr.h`中添加`FP8`的支持（header中已有`FP16`和`FP32`，目的为支持`RGBA8_UNORM`等格式。
4. 在正确load inputs后，在代码中定位并替换对应的的buffers。
5. 在Renderer中能看到正确结果后，添加screenshot输出功能和完整的hack options支持，把之前的compile-time宏定于改成runtime支持（json config or Cmdline args）
6. trace FG相关代码，发现上一步中输出的frames为渲染帧，找到正确的存放生成帧的buffer并输出。
7. 解决其它小问题，如程序自带的imgui window在display resolution（4K）和设备显示器分辨率（2K）不同时的显示bug

## 踩坑记录

### Render Texture vs Content Texture

翻译自[ourREADME.md](./ourREADME.md)中的同名section

SDK中关于Texture的取名会有些困惑，因为两种不同的resources都被`Texure` class表示。以下通过Content和Render来区分他们。

前者为传统意义上的Texture，可以通俗理解为场景贴图，比如Sponza Palace里一块砖的纹理。App默认场景下自带的content textures为.dds格式的文件。

后者不能简单理解为“Render中用到的Texture”，或者再具体一些，“Render中做Texture Sampling的对象”。这个描述属于content texture. Render Texture在SDK中实际上是通用的texture buffer。更准确来说是shader中用到的render-size GPU buffers在CPU端的handle. Render pipeline会向不同的GPU buffers中读写数据，例如FSR中需要的RGB colors, mv, depths. 而他们的CPU handles就是我们需要hack的目标，这样就不用改动相较更复杂的CPU-GPU memory management.

值得注意的是，在**samples/fsrapi**代码中，Render Texture也会被称为Render Targets (RT). 此叫法会更为清晰。此段出自`FSRRenderModule::Init()`

```cpp
    // Fetch needed resources
    m_pColorTarget           = GetFramework()->GetColorTargetForCallback(GetName());
    m_pTonemappedColorTarget = GetFramework()->GetRenderTexture(L"SwapChainProxy");
    m_pDepthTarget           = GetFramework()->GetRenderTexture(L"DepthTarget");
    m_pMotionVectors         = GetFramework()->GetRenderTexture(L"GBufferMotionVectorRT");
    m_pDistortionField[0]    = GetFramework()->GetRenderTexture(L"DistortionField0");
    m_pDistortionField[1]    = GetFramework()->GetRenderTexture(L"DistortionField1");
    m_pReactiveMask          = GetFramework()->GetRenderTexture(L"ReactiveMask");
    m_pCompositionMask       = GetFramework()->GetRenderTexture(L"TransCompMask");
    CauldronAssert(ASSERT_CRITICAL, m_pMotionVectors && m_pDistortionField[0] && m_pDistortionField[1] && m_pReactiveMask && m_pCompositionMask, L"Could not get one of the needed resources for FSR Rendermodule.");
```

在前文实现流程第二步中，我错把两种Texture混为一谈，浪费了许多时间。究其原因，我是从input角度出发，也就是说先在Texture class里
实现了HDR image load. 这一步没有问题，因为已有的LDR image load就在这里。但问题出在我实现和成功使用新的load function后，
我先入为主的认为用类似load接口读取的texture就会有相同的使用。在我们的例子中，LDR image会被用作content texture，
而我们的HDR image应该作为render texture.

### Texture Format

此处具体有两个bug。

第一个为texture description 中的`cauldron::ResourceFormat`变量和pipeline里需要的不对应。
此处并理解问题原因没有花费多长时间，因为SDK中有对应的protection assert，什么variable没有match都非常清晰，是显式错误。

第二个为真正Texture真正的memory layout没有和上面改正确的`ResourceFormat`对应，表现为显示的色调不正确，是隐式错误。
最根本的原因可以总结为C++基础知识不牢固。具体为对C++中不同浮点数精度的处理和类型转换都没有足够深的理解。

之前我只在pytorch中使用过不同精度的float，而转换方法只需要简单设定，类似`dtype=float16`即可。但C++中没有FP16和FP8的原生支持，
需要根据IEEE 754标准，存储bit representation在`uint16_t`和`uint8_t`中。同时，我对`static_cast`和`reinterpret_cast`
的理解也停留在浅显的“保留值不保留精度转换”和“指针类型转换”。但在FP16和FP8情况下二者都不适用，因为我们存储使用的值不是
`uint16_t`和`uint8_t`自身表示的integer value，而是encoded bits. 因此手动实现不同精度float相互的转换。

万幸的是tinyexr中已有定义好的FP32和FP16，只需要仿照着写出FP8和相应的转换即可。

### FG Buffers

在所有基本功能实现后，export FG frames到文件成了最大的难题。由于SDK App本身提供了包含各种options的imgui windows，
其中有一个"Present Interpolated Frame Only"尤其实用，我可以在Renderer里实时看到FG frame.但由于SDK对FSR部分具体
算法实现的封装，追踪并找到真正存放FG frames的buffer非常困难。

因此，我一开始采取的方法是通过输出结果判断，在不同的FG options（例如FG off, FG on, FG on with generated frame only）
下输出截图并用pixel diff比对。这里的致命问题是，当前输出的buffer（`m_pSwapChain`）可以确定被display到屏幕上，但是反过来我却并不能确定
显示器上的每个frame都来自现在这个buffer.简单来说，存放FG frames的buffer和`m_pSwapChain`相互独立，但二者都会被显示。

但退一步说，即便`m_pSwapChain`真的存了我们需要的FG frame，这也是一个不严谨，倒果为因的方法。
手中“不知道是不是FG frame”的export和“没有开FG所以肯定不是FG frame”不一样，也不足够说明它就是FG frame.

最正确的方法还是在明确定义并注释的存放位置。虽然`FSRRenderModule`中只暴露了FG的config setup，
但我们已经可以拿到真正的FG-frame buffer. 具体位置为`ffx::DispatchDescFrameGeneration`中的`outputs`

```cpp
typedef struct ffxDispatchDescFrameGeneration
{
    ffxDispatchDescHeader header;
    void*                 commandList;                ///< The command list on which to register render commands.
    struct FfxApiResource presentColor;               ///< The current presentation color, this will be used as source data.
    struct FfxApiResource outputs[4];                 ///< Destination targets (1 for each frame in numGeneratedFrames).
    uint32_t              numGeneratedFrames;         ///< The number of frames to generate from the passed in color target.
    bool                  reset;                      ///< A boolean value which when set to true, indicates the camera has moved discontinuously.
    uint32_t              backbufferTransferFunction; ///< The transfer function use to convert frame generation source color data to linear RGB. One of the values from FfxApiBackbufferTransferFunction.
    float                 minMaxLuminance[2];         ///< Min and max luminance values, used when converting HDR colors to linear RGB.
    struct FfxApiRect2D   generationRect;             ///< The area of the backbuffer that should be used for generation in case only a part of the screen is used e.g. due to movie bars.
    uint64_t              frameID;                    ///< Identifier used to select internal resources when async support is enabled. Must increment by exactly one (1) for each frame. Any non-exactly-one difference will reset the frame generation logic.
} ffxDispatchDescFrameGeneration;
```

至此，我们才算严谨的输出了一定是我们要的FG frame.

### 其它

一些其它的和FSR无关的小坑也在此做简单记录，作为"lesson for coding in general"

#### 去哪找需要的API

在找到真正FG-frame buffer后，最后一步就是把`FfxApiResource`转化成`GPUResource`格式，后者是现有export to file函数的
使用格式。在相关代码中我已经见过了从`GPUResource`到`FfxApiResource`的转换，但是没见过相反的转换。
幸运的是我在implement前偶然间在不相关的UI Render Module中发现了所需转换的使用。
这告诉我以后遇到类似情况时（对已有的一个或多个class实现“新功能”），一定要先看看相关位置是不是已经有了实现，不要没见过被调用就觉得没有实现。

我这里需要的function就是`GPUResource::GetWrappedResourceFromSDK()`, 而我所需的格式为`GPUResource`,所以在class下面找定义应该是非常自然的事。可我当时只见到了做正向转换的`SDKWrapper::ffxGetResourceApi()`,所以只看了`SDKWrapper`.

#### 合理的runtime option设计

SDK一开始就提供了json config file和Cmdline args两种方法来overwrite默认options. 但显然，前者是更方便且不容易出错的办法。所以当时在把compile-time宏定义改成runtime options的时候，我偷懒只在前者中实现了hack option parsing.

后来我被告知Cmdline args也会大有用处。我们的Offline Runner是可以读取一个场景下的N个连续帧并输出N个生成帧。而当我们要对M个场景进行大规模测试的时候，我们希望可以通过脚本一次跑完并输出**M x N**个结果。此时Cmdline Args就会远比config file方便。而我在开发的时候只拿到了一个test场景，所以忽略了这一点。

一切的feature都有他们存在的理由。在决定不support前一定要全方位分析。

#### Debug最好从上游开始

在流程最后一步解决imgui display bug的时候，我是通过window里的text直接定位到的下游的creation code. 但在debug了一整天后，最后发现fix是在上游UI Render Module里。这种情况其实在bug中非常多见，具体体现为：在改动一个或多个feature之后，用到相关变量的function会出错；而这些变量又会在上游caller和下游utility function都会被用到，这时就最好先fix caller再fix utility.

我这里的具体情况为，因为想用2k的显示器看4k的display resolution，也就是SS之后的大分辨率，我停掉了pipeline中会自动触发的`resizeEvent()`.这使得我们不再有display res == monitor res. 而下游的imgui window creation其实做了正确的scale，但是上游的UI Render Module直接使用了错误的display res.正确的scale只是两行代码，但是意识到imgui的行为还被上游UI RM控制才是关键

### Motion Vector Debug

首先，在App default scene (Sponza Palace)中，使用自带的camera control并查看Frame interpolation debug overlay:
![Frame interpolation debug overlay](docs/techniques/media/frame-interpolation/frame-interpolation-debug-overlay.svg "A diagram showing the debug overlay")

其中左上角Game和右上角Optical Flow的MV保持一致。

- 紫红色：camera向右上，物体向左下
- 蓝色：camera向左上，物体向右下
- 黄色：camera向右下，物体向左上
- 绿色：camera向左下，物体向右上

随后对我们的MV input进行手动调整，发现negate MV.x并保留MV.y不变，才能使两种MV颜色匹配。这时从杨博文处了解到，我们的input在创建时做了一次y方向的negation。

假设某个pixel在frame N所需的正确MV为 (X, Y), 反向推断出其在UE引擎生成时值为 (-X, -Y), 因为MTSS team对y进行取反，我们读取时对x进行取反。因此我们猜测二者的reference framework不一致，一个为frame N -> N+1,另一个刚好相反。

为验证此猜想，我实现了MV export接口，并给camera加入向左上的缓慢移动使物体向右下移动，通过debug overlay里面MV呈蓝色确认。之后读取export MV images的pixel data，发现x和y值都为负数。这里说明对于物体移动来说，在使用NDC或者pixel coordinate的情况下（都是右下为正方向）在FSR App里的MV是 (Frame N - FrameN+1)。这也和官方doc里的解释对应：

> For example, a motion vector for a pixel in the upper-left corner of the screen with a value of <width, height> would represent a motion that traversed the full width and height of the input surfaces, originating from the bottom-right corner.

之后我切换回了我们的test scene，使用MVD里对应Frame 1的2473（这里没有使用对应Frame 0的2472，因为发现其MV值和之后的几帧相反，可能是“不存在的”Frame 负1到Frame 0的MV，不可靠）。比较人物无名指位置，pixel (1110, 535) ：input中data为 (0.02, -0.08)，而exported MV中此值为 (-0.02, -0.08)。结合test scene人物的动画手部是向右下移动，得出结论：

1. 原始MV数据从UE引擎生成时符合confluence里所说的NDC坐标，右下方向为正，同时取值是common-sense的（Frame N+1 - Frame N）。
2. FSR App所用的MV正好相反，取值是（Frame N - Frame N+1）。
